- name: Base config on all machines
  hosts: all
  roles:
    - common

# JLBCredit forest root
- name: Create JLBCredit forest root DC
  hosts: dc_jlb_root
  roles:
    - dc_root

# Join workstations
- name: Join JLBCredit workstations
  hosts: workstations_jlb
  roles:
    - workstation

# AD config (users, OUs, RDP)
- name: Configure AD
  hosts: dc_jlb_root
  roles:
    - ad_config
